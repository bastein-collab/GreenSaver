name: Ingest NJ Stores
on:
schedule:
- cron: '*/15 * * * *' # every 15 minutes (chunked)
workflow_dispatch:

jobs:
ingest:
runs-on: ubuntu-latest
permissions:
contents: read

name: Ingest NJ Stores
on:
schedule:
- cron: '*/15 * * * *' # every 15 minutes (chunked)
workflow_dispatch:

jobs:
ingest:
runs-on: ubuntu-latest
permissions:
contents: read
